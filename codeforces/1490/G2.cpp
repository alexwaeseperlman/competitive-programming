#include <bits/stdc++.h>
using namespace std;

void solve() {
	int n, m; cin >> n >> m;
	vector<int> tape(n);

	for (int i = 0; i < n; i++) {
		cin >> tape[i];
	}
}

int main() {
	int t; cin >> t;
	while (t--) solve();
}
